rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    // CHỈ để test: cho phép mọi collection nếu đã đăng nhập
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
